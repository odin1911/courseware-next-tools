# PNG 图集优化方案

## 1. 结论

当前三格式分流继续保持不变：

- Chrome / Android：VP9 Alpha WebM。
- Safari / iOS 13+：HEVC Alpha MOV。
- iOS 12或视频播放失败：透明 PNG 图集。

iOS 12是当前模板的固定兼容要求，因此 PNG 图集不能取消。但当前图集存在大量重复帧和透明空白，具备明显的结构性优化空间。

推荐优先实施：

1. 精确重复帧去重。
2. 每个动作独立裁切。
3. 使用 `frameMap` 保存时间轴映射。
4. 在保持画质的前提下进行 PNG 压缩。

如果第一阶段仍不能满足体积要求，再实施逐帧裁切和可变尺寸图集。

SVG 不适合作为透明视频面向 iOS 12的自动降级格式。

## 2. 当前数据

分析对象为当前14个动画资源、2,313个动态帧。

| 优化方式 | 理论像素保留量 | 理论减少 |
| --- | ---: | ---: |
| 当前固定画布 | 100% | - |
| 仅精确重复帧去重 | 62.1% | 37.9% |
| 去重 + 每个动作统一裁切 | 30.6% | 69.4% |
| 去重 + 每帧独立裁切 | 21.4% | 78.6% |

以上是像素面积估算，不代表 PNG 文件会按相同比例缩小。实际文件体积还取决于颜色复杂度、透明度、重复纹理和压缩器。

补充发现：

- 当前所有动态帧中，真正非透明的像素约占19.5%。
- 2,313帧中只有1,210个精确唯一帧，重复帧占47.7%。
- 当前图集尾行和空槽带来的额外像素约为5.2%。
- `BD_laki/wait` 共146帧，只有25个精确唯一帧，可去重82.9%。
- `BD_flash/start` 当前画布为890×632，按动作裁切后只需保留约18.7%的画布面积。

当前 PNG 降级资源合计约26.69 MB，是三格式发布目录中最大的组成部分。详细原始统计参见 [视频动画资源体积统计](./src/pages/KJG_QAP_BD_v2_2026_video/video-animation-metrics.md)。

## 3. 当前体积问题的原因

当前生成流程对同一资源的全部动作使用统一的 `canvas` 和 `anchor`。这种方式能够避免动作切换时发生位置跳动，但也会导致：

- 小动作继承资源中最大动作的画布。
- 每一帧重复保存相同的透明空白。
- 长时间保持不动的动作仍按24 fps保存重复帧。
- 图集使用固定大小槽位，无法利用每帧不同的透明边界。
- 最后一页不足整行时仍可能保留空槽。

现有画布和锚点规则参见 [视频生成流程](./tools/ai-animation/video-generation-pipeline.md)。

## 4. 推荐的视频转 PNG 流程

```text
AI Alpha 视频
    ↓
按目标帧率解码为 RGBA 帧
    ↓
校验尺寸、帧数和 Alpha
    ↓
检测每帧透明边界
    ↓
重复帧去重并记录持续帧数
    ↓
动作级或逐帧裁切
    ↓
可变尺寸图集打包
    ↓
PNG 压缩和可选索引色量化
    ↓
生成带 rect、offset、frameMap 的 manifest
```

如果 AI 生产工具能够直接输出无损 RGBA 帧，应优先使用帧目录，不要先压缩成有损视频再解码。

## 5. 第一阶段：重复帧去重 + 动作级裁切

这是推荐首先实施的方案，理论像素面积可减少约69.4%，同时保持播放器模型相对简单。

### 5.1 重复帧去重

对每个动作的 RGBA 帧计算哈希：

- 完全相同的帧只在图集中保存一次。
- 时间轴仍保留原帧数。
- `frameMap` 把每个时间轴帧映射到唯一图块。
- 连续重复帧也可以压缩成 `durationFrames`。

示例：

```json
{
  "frameMap": [0, 0, 0, 1, 2, 2, 3],
  "uniqueFrameCount": 4,
  "frameCount": 7
}
```

### 5.2 每个动作独立裁切

当前 `canvas` 和 `anchor` 位于资源级。优化后，每个动作应拥有自己的裁切尺寸和锚点：

```json
{
  "canvas": { "width": 120, "height": 160 },
  "anchor": { "x": -76, "y": -154 },
  "frameCount": 146,
  "frameMap": []
}
```

动作级裁切仍使用固定动作画布，因此动作内部不会抖动。动作切换时，通过动作自己的 `anchor` 恢复到原始屏幕坐标。

位置计算保持为：

```text
屏幕位置 = 模板 origin + 动作 anchor
```

## 6. 第二阶段：逐帧裁切 + 可变尺寸图集

如果第一阶段的体积仍不能满足要求，可以让每个唯一帧拥有独立尺寸和偏移。

```json
{
  "page": 0,
  "rect": [320, 160, 84, 132],
  "offset": [18, 24],
  "durationFrames": 4
}
```

字段含义：

- `page`：所在图集页。
- `rect`：图集内的 `x / y / width / height`。
- `offset`：该裁切帧在动作原始画布中的位置。
- `durationFrames`：该图块应保持的时间轴帧数。

播放器每帧需要：

1. 清空目标 Canvas。
2. 根据 `frameMap` 找到唯一帧。
3. 从图集的 `rect` 读取变长矩形。
4. 按 `offset` 绘制到动作画布。
5. 根据原始 fps 和持续帧数计算完成或循环状态。

逐帧裁切理论上可以把当前像素面积减少约78.6%，但 manifest 和播放器的改动更大，并且必须重点验证 offset，避免出现位置抖动。

## 7. 视频输入下的重复帧判断

当前资源来自 DragonBones 无损帧，因此能够发现大量字节完全一致的重复帧。未来从有损视频解码时，相同视觉帧可能包含轻微编码噪声。

推荐两级判断：

1. 优先使用 RGBA 字节哈希进行无风险精确去重。
2. 可选使用严格的近似重复判断，但必须同时比较 RGB 和 Alpha。

近似去重需要限制：

- 分别设置颜色和 Alpha 最大误差。
- 对半透明边缘使用更严格阈值。
- 光效、粒子、渐变和运动模糊默认不做近似去重。
- 用 Golden Master 逐帧差异图验证。
- 任一帧超过阈值时保留原帧。

## 8. PNG 编码优化

### 8.1 无损重压缩

可以在图集生成后使用更强的 PNG 压缩器，并且只保留体积更小的结果。

当前抽样结果约为0～10%的体积变化，收益低于裁切和去重，应放在结构优化之后。

### 8.2 PNG8 索引色

PNG8 可以保存调色板及透明度，对颜色较少的卡通角色可能有明显收益，但最多只有256组 RGBA 组合。

使用条件：

- 为同一动作的所有图集页生成共享调色板，减少跨页颜色跳变。
- 对渐变、光效和半透明边缘单独设定质量门槛。
- 量化失败或差异超过阈值时自动保留 RGBA PNG。
- 不将 GIF 的单级透明处理方式用于 PNG8；必须保留多级 Alpha。

### 8.3 尾页紧凑布局

最后一页不足整行时，应按实际使用列数缩小图集宽度。该项对当前全部资源的理论像素收益约5.2%，可作为低风险补充优化。

## 9. 运行时与内存要求

- 图集继续按动作、按页懒加载，不能一次加载完整 PNG 降级资源。
- 单页尺寸继续限制在2048×2048以内。
- 一张2048×2048 RGBA 图片解码后理论上约占16 MiB内存。
- 动作切换后应释放播放器对旧图集页的强引用。
- 循环动作跨图集页时，可预取下一页，避免页边界卡顿。
- 不应把 WebM、MOV 和全部 PNG 图集同时加入预缓存。

## 10. SVG 可行性

SVG 不适合作为“透明视频 → iOS 12降级动画”的自动产物。

视频解码后只有 RGBA 像素，没有骨骼、路径、图层或关键帧语义：

| 方案 | 问题 |
| --- | --- |
| 每帧自动描摹为 SVG 路径 | 路径数量巨大；渐变、模糊和粒子失真；相邻帧路径变化会闪烁 |
| 在 SVG 中嵌入 PNG 帧 | PNG 数据仍然存在，体积不会降低 |
| 用 SVG / CSS 重制 | 属于重新制作动画，不能从视频稳定自动恢复 |
| AI 直接生成 SVG / Lottie | 只适合简单图标、几何形状和明确矢量图层的动画 |

复杂角色、粒子、光效和半透明边缘仍应使用视频或优化后的 PNG 图集。只有当上游从一开始就提供真实矢量素材时，SVG 才值得作为独立生产路径评估。

## 11. 实施优先级

### P0：先做

- 精确重复帧去重。
- 动作级透明边界裁切。
- manifest 增加动作级 `canvas / anchor / frameMap`。
- 保持 PNG 图集懒加载和视频失败续播。

### P1：数据不足时再做

- 逐帧裁切。
- 可变尺寸图集打包。
- `rect / offset / durationFrames` 播放模型。
- 跨页预取。

### P2：作为压缩补充

- 无损 PNG 重压缩。
- 带质量门槛的 PNG8 量化。
- 尾页紧凑布局。

## 12. 验收标准

- WebM 和 MOV 路径行为不变。
- iOS 12仍稳定使用 PNG Canvas。
- 视频失败后能从相邻帧切换到 PNG，不重新播放。
- 动作位置、大小、时长、循环和完成回调与优化前一致。
- 动作切换无跳位，逐帧播放无抖动。
- Alpha 边缘没有黑边、白边、色阶断层或闪烁。
- 记录每个资源优化前后的 PNG 字节数、图集页数、首帧时间和峰值内存。
- 使用当前全量资源重新统计，确认实际收益而不是只依据理论像素面积。
